<UserControl
    x:Class="User.CornerSpeed.iRacingCornerSpeedDelta"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:User.CornerSpeed"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:iRacingCornerSpeedDeltaItem}"
    d:DesignHeight="65"
    d:DesignWidth="600"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/User.CornerSpeed;component/App.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="DeltaBarTextBlockStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontFamily" Value="Inter" />
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontFamily" Value="Chivo Mono" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="TextAlignment" Value="Center" />
            </Style>
            <Style
                x:Key="DeltaBarProgressBarStyle"
                BasedOn="{StaticResource {x:Type Control}}"
                TargetType="{x:Type ProgressBar}">
                <Setter Property="OverridesDefaultStyle" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <ControlTemplate.Resources>
                                <Storyboard x:Key="Anim.Indeterminate">
                                    <DoubleAnimation
                                        RepeatBehavior="Forever"
                                        Storyboard.TargetName="IndicatorIndeterminate"
                                        Storyboard.TargetProperty="Background.RelativeTransform.X"
                                        From="-1"
                                        To="1"
                                        Duration="0:0:2" />
                                </Storyboard>
                            </ControlTemplate.Resources>
                            <Grid
                                x:Name="TemplateRoot"
                                MinHeight="4"
                                ClipToBounds="True">
                                <Border
                                    x:Name="PART_Track"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    ClipToBounds="True"
                                    CornerRadius="{TemplateBinding Border.CornerRadius}">
                                    <Grid ClipToBounds="True">
                                        <Border
                                            x:Name="PART_Indicator"
                                            HorizontalAlignment="Left"
                                            Background="{TemplateBinding Foreground}"
                                            ClipToBounds="True"
                                            CornerRadius="{TemplateBinding Border.CornerRadius}" />
                                        <Border
                                            x:Name="IndicatorIndeterminate"
                                            ClipToBounds="True"
                                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                                            Visibility="Collapsed">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                    <LinearGradientBrush.RelativeTransform>
                                                        <TranslateTransform />
                                                    </LinearGradientBrush.RelativeTransform>
                                                    <GradientStop Offset="0" Color="{Binding Background.Color, RelativeSource={RelativeSource TemplatedParent}}" />
                                                    <GradientStop Offset="0.5" Color="{Binding Foreground.Color, RelativeSource={RelativeSource TemplatedParent}}" />
                                                    <GradientStop Offset="1" Color="{Binding Background.Color, RelativeSource={RelativeSource TemplatedParent}}" />
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                    </Grid>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Orientation" Value="Vertical">
                                    <Setter TargetName="TemplateRoot" Property="LayoutTransform">
                                        <Setter.Value>
                                            <RotateTransform Angle="-90" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsIndeterminate" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard Storyboard="{StaticResource Anim.Indeterminate}" />
                                    </Trigger.EnterActions>
                                    <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="IndicatorIndeterminate" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Border.DisabledChecked}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Border.CornerRadius" Value="0" />
                <Setter Property="FlowDirection" Value="LeftToRight" />
                <Setter Property="Minimum" Value="0" />
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Border.CornerRadius" Value="0,8,8,0" />
            </Style>
            <Style x:Key="DeltaBarTextStyle" TargetType="{x:Type ContentControl}">
                <Setter Property="Border.BorderBrush" Value="{StaticResource BlackboxBorderBrush}" />
                <Setter Property="Border.CornerRadius" Value="8" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="4" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ContentControl}">
                            <Grid>
                                <Border
                                    x:Name="OuterBorder"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding Border.CornerRadius}"
                                    Opacity="1.0"
                                    Style="{StaticResource BlackboxBorderStyle0}" />
                                <Border
                                    Background="{DynamicResource BlackboxBackgroundBrush1}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding Border.CornerRadius}"
                                    Opacity="1.0"
                                    Style="{StaticResource BlackboxBorderStyle1}" />
                                <Border
                                    x:Name="AccentBorder"
                                    CornerRadius="{TemplateBinding Border.CornerRadius}"
                                    Opacity="1.0" />
                                <ContentPresenter Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel
        Background="Transparent"
        MouseDown="UserControl_MouseDown"
        Orientation="Vertical">
        <ContentControl
            x:Name="DeltaBar"
            Width="600"
            Height="16"
            Padding="1"
            Background="{StaticResource BlackboxBackgroundBrush}"
            ClipToBounds="True"
            Style="{StaticResource BlackboxControlStyle}">
            <Grid Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ProgressBar
                    x:Name="RedDeltaBar"
                    Grid.Row="0"
                    Grid.Column="0"
                    Foreground="{Binding DDelta, Converter={StaticResource DeltaBarColorConverter}}"
                    Maximum="{Binding Range}"
                    Style="{StaticResource DeltaBarProgressBarStyle}"
                    Value="{Binding Delta}">
                    <ProgressBar.LayoutTransform>
                        <ScaleTransform CenterX="0.5" ScaleX="-1" />
                    </ProgressBar.LayoutTransform>
                </ProgressBar>
                <ProgressBar
                    x:Name="GreenDeltaBar"
                    Grid.Row="0"
                    Grid.Column="2"
                    Foreground="{Binding DDelta, Converter={StaticResource DeltaBarColorConverter}}"
                    Maximum="{Binding Range}"
                    Style="{StaticResource DeltaBarProgressBarStyle}"
                    Value="{Binding Delta, Converter={StaticResource ValueToInverseConverter}}">
                    <ProgressBar.LayoutTransform>
                        <ScaleTransform CenterX="0.5" ScaleX="1" />
                    </ProgressBar.LayoutTransform>
                </ProgressBar>
            </Grid>
        </ContentControl>
        <ContentControl
            MinWidth="0"
            MinHeight="0"
            Padding="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Style="{StaticResource DeltaBarTextStyle}">
            <TextBlock
                Margin="4,6"
                Foreground="{Binding Delta, Converter={StaticResource DeltaBarColorConverter}}"
                Style="{StaticResource DeltaBarTextBlockStyle}"
                Text="{Binding Delta, Converter={StaticResource LapTimeDeltaConverter}}" />
        </ContentControl>
    </StackPanel>
</UserControl>
